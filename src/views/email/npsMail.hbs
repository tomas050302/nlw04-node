<div class="container">
  <label>Hello <strong>{{name}}</strong>!</label>

  <h3>{{title}}</h3><br>
  <strong>{{description}}</strong>


  <div class='recommendation-degree'>
    <span>Not recommended</span>
    <span>I will recommend</span>
  </div>

  <div class='answers'>
    <a href="{{url}}/answers/1?user={{user_id}}">1</a>
    <a href="{{url}}/answers/2?user={{user_id}}">2</a>
    <a href="{{url}}/answers/3?user={{user_id}}">3</a>
    <a href="{{url}}/answers/4?user={{user_id}}">4</a>
    <a href="{{url}}/answers/5?user={{user_id}}">5</a>
    <a href="{{url}}/answers/6?user={{user_id}}">6</a>
    <a href="{{url}}/answers/7?user={{user_id}}">7</a>
    <a href="{{url}}/answers/8?user={{user_id}}">8</a>
    <a href="{{url}}/answers/9?user={{user_id}}">9</a>
    <a href="{{url}}/answers/10?user={{user_id}}">10</a>
  </div>

  <br>
  <strong>Your opinion is very important to us!</strong>
  <h3>K7 | <strong> INC</strong></h3>
</div>


<style>
  .container {
    display: flex;
    flex-direction: column;

    width: 800px;
    justify-content: center;
    align-items: center;
    align-content: center;
  }

  .answers {
    width: 350px;
    display: flex;
    justify-content: space-between;
  }

  .answers a {
    padding: 10px;
    background: #8257e6;
    border-radius: 6px;
    width: 15px;

    text-align: center;
    text-decoration: none;
  }

  .recommendation-degree {
    width: 350px;

    display: flex;
    justify-content: space-between;

    margin: 10px;
  }
</style>
